<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8" />
<title>Valor Wellness Retreat - Listen Within</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Lato:wght@300;400;700&display=swap');

  :root {
    --primary-orange: #CC5500;
    --deep-orange: #A04100;
    --accent-gold: #DAB83C;
    --warm-brown: #8B4513;
    --deep-brown: #4f1e0c;
    --light-cream: #F5F5DC;
    --warm-cream: #EDE8D1;
    --action-card-bg: #E0E0E0;
    --icon-color: #4f1e0c;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: linear-gradient(135deg, #FDF6E3, #FFE4B5, #FFF8DC);
    font-family: '(Arial Rounded MT Bolt);
    color: var(--deep-brown);
    line-height: 1.6;
    overflow-x: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
  }

  /* Particles background */
  .particles {
    position: fixed;
    top: 0; left: 0; width: 100%; height: 100%;
    pointer-events: none;
    z-index: -1;
  }
  .particle {
    position: absolute;
    width: 4px; height: 4px;
    background: var(--accent-gold);
    border-radius: 50%;
    opacity: 0.6;
    animation: float 6s ease-in-out infinite;
  }
  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); opacity: 0.6; }
    50% { transform: translateY(-20px) rotate(180deg); opacity: 1; }
  }

 /* Header styles */
  .header {
    text-align: center;
    padding: 40px 20px;
    position: relative;
    overflow: hidden;
  }
  .header::before {
    content: '';
    position: absolute;
    top: -50%; left: -50%;
    width: 200%; height: 200%;
    background: radial-gradient(circle, rgba(218, 184, 60, 0.1) 0%, transparent 70%);
    animation: rotate 20s linear infinite;
  }
  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .logo-container {
    position: relative;
    margin-bottom: 20px;
    display: inline-block;
  }
  .logo-circle {
    width: 120px; height: 120px;
    border: 3px solid var(--accent-gold);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 20px;
    background: linear-gradient(135deg, rgba(218, 184, 60, 0.2), rgba(139, 69, 19, 0.1));
    box-shadow: 0 8px 32px rgba(218, 184, 60, 0.3);
    animation: pulse 3s ease-in-out infinite;
  }
  @keyframes pulse {
    0%, 100% { transform: scale(1); box-shadow: 0 8px 32px rgba(218, 184, 60, 0.3); }
    50% { transform: scale(1.05); box-shadow: 0 12px 40px rgba(218, 184, 60, 0.5); }
  }
  .logo-img {
    width: 80px; height: 80px; object-fit: contain;
  }

  .header h1 {
    font-family: '(Arial Rounded MT Bolt)', serif;
    font-weight: 900;
    font-size: 3.5em;
    color: var(--warm-brown);
    margin-bottom: 10px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    position: relative; z-index: 1;
  }

  .header .tagline {
    font-size: 1.4em; font-weight: 300; color: var(--deep-brown);
    margin-bottom: 10px; opacity: 0.9;
  }

  .header .subtitle {
    font-size: 1.1em; color: var(--warm-brown);
    font-style: italic; border-bottom: 2px solid var(--accent-gold);
    display: inline-block; padding-bottom: 5px;
  }

  /* Quote card styles */
  .quote-card {
    background: linear-gradient(135deg, rgba(237, 232, 209, 0.95), rgba(245, 245, 220, 0.9));
    padding: 40px; border-radius: 20px;
    margin: 40px 0; box-shadow: 0 12px 40px rgba(0,0,0,0.15);
    font-style: italic; font-size: 1.4em; color: var(--deep-brown);
    border-left: 6px solid var(--accent-gold);
    position: relative; overflow: hidden;
    opacity: 0; transform: translateY(20px);
    animation: slideUp 1s ease-out 0.5s forwards;
  }
  @keyframes slideUp {
    to { transform: translateY(0); opacity: 1; }
  }
  .quote-card::before {
    content: '"';
    font-size: 6em; color: var(--accent-gold);
    position: absolute; top: -10px; left: 20px;
    opacity: 0.3;
    font-family: 'Playfair Display', serif;
  }

  /* Action grid styles */
  .actions {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px; margin: 50px 0;
  }

  .card {
    background: var(--action-card-bg); color: var(--deep-brown);
    border-radius: 20px; padding: 30px; text-align: center; cursor: pointer;
    box-shadow: 0 8px 32px rgba(0,0,0,0.2);
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    position: relative; overflow: hidden;
    opacity: 0; transform: translateY(30px);
    animation: cardSlideUp 1s ease-out forwards;
  }
  .card:nth-child(1) { animation-delay: 0.2s; }
  .card:nth-child(2) { animation-delay: 0.4s; }
  .card:nth-child(3) { animation-delay: 0.6s; }
  @keyframes cardSlideUp {
    to { opacity: 1; transform: translateY(0); }
  }
  .card:hover {
    transform: translateY(-8px) scale(1.02);
    box-shadow: 0 16px 48px rgba(0,0,0,0.3);
  }

  /* Icons in cards */
  .icon {
    font-size: 3.5em; margin-bottom: 15px; display: block;
    color: var(--icon-color); fill: currentColor;
  }

  /* Titles and paragraphs in cards */
  .card h3 {
    margin: 15px 0 10px; font-weight: 700; font-size: 1.3em; font-family: 'Playfair Display', serif;
  }
  .card p {
    opacity: 0.9; line-height: 1.5;
  }

  /* Featured section styles */
  .featured-section {
    background: linear-gradient(135deg, rgba(237, 232, 209, 0.8), rgba(245, 245, 220, 0.6));
    border-radius: 25px; padding: 40px; margin: 50px 0;
    box-shadow: 0 12px 40px rgba(0,0,0,0.1);
    display: grid; grid-template-columns: 1fr 1fr; gap: 40px; align-items: center;
  }
  .featured-content {
    padding: 20px;
  }
  .featured-content h2 {
    font-family: 'Playfair Display', serif; color: var(--warm-brown);
    margin-bottom: 20px; font-size: 2.2em; text-align: left;
  }
  .featured-content p {
    font-size: 1.1em; color: var(--deep-brown); line-height: 1.6; text-align: left;
  }

  /* Serenity image styles */
  .featured-image {
    max-width: 100%; height: 400px;
    background-image: url('https://valor.com.na/wp-content/uploads/2025/05/WhatsApp-Image-2025-05-09-at-08.05.47-4.jpeg');
    background-size: cover; background-position: center;
    border-radius: 20px; box-shadow: 0 12px 40px rgba(0,0,0,0.2);
    transition: transform 0.3s ease;
  }
  .featured-image:hover {
    transform: scale(1.02);
  }

  /* Quote section styles */
  .main-quote {
    font-family: 'Playfair Display', serif; font-style: italic; font-size: 1.6em;
    text-align: center; margin: 50px 0; padding: 30px;
    background: linear-gradient(135deg, rgba(218, 184, 60, 0.1), rgba(139, 69, 19, 0.05));
    border-radius: 20px; border-left: 6px solid var(--accent-gold); position: relative;
  }

  /* Responsive adjustments */
  @media(max-width: 768px){
    .header h1 { font-size: 2.5em; }
    .actions { grid-template-columns: 1fr; }
    .featured-section { grid-template-columns: 1fr; }
    .featured-content h2 { text-align: center; }
    .featured-content p { text-align: center; }
  }
</style>
</head>
<body>
<div class="particles" id="particles"></div>

<div class="container fade-in">
  <!-- Logo section -->
  <div class="header">
    <div class="logo-container">
      <div class="logo-circle">
        <img src="https://valor.com.na/wp-content/uploads/2025/05/cropped-icon.png" alt="VWR Logo" class="logo-img" />
      </div>
    </div>
    <h1>VALOR</h1>
    <p class="tagline">Wellness Retreat</p>
    <p class="subtitle">Listen Within</p>
  </div>

  <!-- Quote -->
  <div class="main-quote">
    "Wellness is not a destination, it's a way of life. Let us guide you on your transformative journey to inner peace and vitality."
  </div>

  <!-- Action Cards -->
  <div class="actions">
    <div class="card" onclick="window.open('https://valor.com.na/', '_blank')">
      <div class="icon">
        <!-- Website SVG icon -->
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="var(--icon-color)" viewBox="0 0 24 24">
          <path d="M12 20c4.41 0 8-3.59 8-8s-3.59-8-8-8-8 3.59-8 8 3.59 8 8 8zm0-14c3.31 0 6 2.69 6 6s-2.69 6-6 6-6-2.69-6-6 2.69-6 6-6zm0 10c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0-14C6.48 2 2 6.48 2 12h2c0-4.41 3.59-8 8-8s8 3.59 8 8c0 4.41-3.59 8-8 8s-8-3.59-8-8H2c0 5.52 4.48 10 10 10s10-4.48 10-10S17.52 2 12 2z"/>
        </svg>
      </div>
      <h3>Visit Our Website</h3>
      <p>Explore our comprehensive wellness programs and facilities designed for your healing journey.</p>
    </div>
    <div class="card" onclick="window.open('https://online.fliphtml5.com/asohk/soxh/', '_blank')">
      <div class="icon">
        <!-- Brochure SVG icon -->
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="var(--icon-color)" viewBox="0 0 24 24">
          <path d="M4 4h16v2H4V4zm0 4h16v12H4V8zm2 2v8h12v-8H6z"/>
        </svg>
      </div>
      <h3>View Our Brochure</h3>
      <p>Discover detailed information about our retreat offerings and therapeutic services.</p>
    </div>
    <div class="card" onclick="window.open('https://valor.com.na/', '_blank')">
      <div class="icon">
        <!-- Referral SVG icon -->
        <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="var(--icon-color)" viewBox="0 0 24 24">
          <path d="M12 12c2.21 0 4-1.79 4-4S14.21 4 12 4 8 5.79 8 8s1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
      </div>
      <h3>Referral Form</h3>
      <p>Download our professional referral form for healthcare providers.</p>
    </div>
  </div>

  <!-- Featured section -->
  <div class="featured-section">
    <div class="featured-content">
      <h2>Experience Transformative Wellness</h2>
      <p>Nestled in nature's embrace, our retreat offers a sacred space for healing, growth, and self-discovery. Every moment here is designed to nurture your body, mind, and spirit.</p>
    </div>
    <div class="featured-image"></div>
  </div>
</div>

<script>
// Generate animated particles background
function createParticles() {
  const container = document.getElementById('particles');
  const count = 20;
  for (let i = 0; i < count; i++) {
    const p = document.createElement('div');
    p.className = 'particle';
    p.style.left = Math.random() * 100 + '%';
    p.style.top = Math.random() * 100 + '%';
    p.style.animationDelay = Math.random() * 6 + 's';
    p.style.animationDuration = (Math.random() * 4 + 4) + 's';
    container.appendChild(p);
  }
}
document.addEventListener('DOMContentLoaded', createParticles);
</script>
</body>
</html>
